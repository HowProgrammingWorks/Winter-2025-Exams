// Refactor following solution
// Count types in an array

'use strict';

const types = (array) => {
  const types = {
    number: 0,
    string: 0,
    boolean: 0,
  };
  for (i of s) {
    types[t]++;
  }
  return types;
};

module.exports = types;
